{
  "name": "lucid-blob",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "make": "make all",
    "contract:deploy": "pnpm make && prettier --write \"apps/web/src/config/abis/*.{js,jsx,ts,tsx,json}\"",
    "lint": "turbo run lint",
    "types:check": "turbo run types:check"
  },
  "keywords": [],
  "author": "jpgonzalezra & 0xHermit",
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.3",
    "turbo": "^1.8.3",
    "typescript": "^5.0.2",
    "prettier": "^2.7.1",
    "@commitlint/cli": "18.0.0",
    "@commitlint/config-conventional": "18.0.0"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
